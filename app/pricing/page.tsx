import Link from "next/link";
import { PLAN_DEFINITIONS, getPlanFeatures, type PlanTier } from "@/lib/plans";
import { BILLING_CONFIG } from "@/services/billing/config";
import { COLORS } from "@/lib/design-tokens";

export default function PricingPage(){const tiers:PlanTier[]=["FREE","PRO","PARTNER"];return <main style={{minHeight:"100vh",background:COLORS.paper,padding:"72px 24px"}}><div style={{maxWidth:1100,margin:"0 auto"}}><div style={{textAlign:"center",maxWidth:680,margin:"0 auto 42px"}}><div style={{fontSize:13,color:COLORS.blueDeep,fontWeight:600}}>PLANES</div><h1 style={{fontSize:"clamp(34px,5vw,54px)",letterSpacing:"-0.04em",margin:"12px 0"}}>Un plan para cada etapa</h1><p style={{color:COLORS.inkSoft,lineHeight:1.7}}>Empezá con un diagnóstico base y ampliá capacidades cuando necesites gestionar más marketing, negocios o clientes.</p></div><div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(270px,1fr))",gap:18}}>{tiers.map(tier=>{const plan=PLAN_DEFINITIONS[tier];return <article key={tier} style={{background:"#fff",border:`1px solid ${tier==="PRO"?COLORS.blue:COLORS.line}`,borderRadius:18,padding:26}}><h2 style={{fontSize:24,margin:0}}>{plan.label}</h2><div style={{fontSize:18,fontWeight:650,marginTop:12}}>{BILLING_CONFIG.displayPrices[tier]}</div><p style={{fontSize:13,color:COLORS.inkSoft,lineHeight:1.6,minHeight:64}}>{plan.audience}</p><ul style={{paddingLeft:18,display:"grid",gap:9,fontSize:13,lineHeight:1.5}}>{plan.highlights.map(item=><li key={item}>{item}</li>)}</ul><div style={{borderTop:`1px solid ${COLORS.line}`,marginTop:20,paddingTop:14,fontSize:12,color:COLORS.inkFaint}}>{getPlanFeatures(tier).length} capacidades incluidas</div></article>})}</div><div style={{textAlign:"center",marginTop:34}}><Link href="/register" style={{display:"inline-block",padding:"12px 20px",borderRadius:10,background:COLORS.blue,color:"#fff",textDecoration:"none",fontWeight:600}}>Crear cuenta</Link></div></div></main>}
