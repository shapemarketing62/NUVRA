"use client"; import { FormEvent, useState } from "react"; import { BrandMark, Btn, Card, Field, TextInput } from "@/components/ui"; import { COLORS } from "@/lib/design-tokens";
export default function ForgotPasswordPage(){const[email,setEmail]=useState("");const[message,setMessage]=useState("");const[devLink,setDevLink]=useState("");async function submit(e:FormEvent){e.preventDefault();const r=await fetch("/api/auth/password-reset/request",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email})});const b=await r.json();setMessage(b.message||"Si la cuenta existe, vas a recibir instrucciones.");setDevLink(b.recoveryUrl||"");}return <main style={{minHeight:"100vh",display:"grid",placeItems:"center",padding:24}}><div style={{width:"100%",maxWidth:430}}><div style={{display:"flex",justifyContent:"center",marginBottom:24}}><BrandMark/></div><Card><h1 className="section-title" style={{fontSize:24}}>Recuperar contraseña</h1><p style={{color:COLORS.inkSoft,fontSize:13,lineHeight:1.6,margin:"8px 0 24px"}}>Ingresá tu email para recibir un enlace de recuperación.</p><form onSubmit={submit}><Field label="Email"><TextInput value={email} onChange={setEmail} type="email"/></Field><Btn type="submit" full>Enviar instrucciones</Btn></form>{message&&<p style={{fontSize:13,color:COLORS.inkSoft,marginTop:18}}>{message}</p>}{devLink&&<a href={devLink} style={{display:"block",fontSize:12,color:COLORS.blue,marginTop:12}}>Abrir enlace de desarrollo</a>}</Card></div></main>}
