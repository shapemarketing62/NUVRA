"use client"; import { FormEvent,Suspense,useState } from "react"; import { useSearchParams } from "next/navigation"; import { Btn,Card,Field,TextInput } from "@/components/ui"; import { COLORS } from "@/lib/design-tokens";
function ResetPasswordForm(){const token=useSearchParams().get("token")||"";const[p,setP]=useState("");const[msg,setMsg]=useState("");async function submit(e:FormEvent){e.preventDefault();const r=await fetch("/api/auth/password-reset/confirm",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({token,password:p})});setMsg(r.ok?"Contraseña actualizada. Ya podés ingresar.":"El enlace venció o ya fue utilizado.");}return <main style={{minHeight:"100vh",display:"grid",placeItems:"center",padding:24}}><Card style={{width:"100%",maxWidth:430}}><h1 className="section-title" style={{fontSize:24}}>Nueva contraseña</h1><form onSubmit={submit} style={{marginTop:24}}><Field label="Contraseña" hint="Usá al menos 10 caracteres."><TextInput value={p} onChange={setP} type="password"/></Field><Btn type="submit" full disabled={!token}>Guardar contraseña</Btn></form>{msg&&<p style={{fontSize:13,color:COLORS.inkSoft,marginTop:18}}>{msg}</p>}</Card></main>}
export default function ResetPasswordPage(){return <Suspense fallback={null}><ResetPasswordForm/></Suspense>}
