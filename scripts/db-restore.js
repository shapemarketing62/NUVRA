const fs=require("node:fs"),path=require("node:path"),crypto=require("node:crypto");const root=path.resolve(__dirname,".."),backup=process.argv[2]&&path.resolve(process.argv[2]),confirm=process.argv.includes("--confirm"),target=path.join(root,"prisma","dev.db");if(!backup||!confirm)throw new Error("Usage: node scripts/db-restore.js <backup.db> --confirm");const manifest=JSON.parse(fs.readFileSync(`${backup}.json`,"utf8")),actual=crypto.createHash("sha256").update(fs.readFileSync(backup)).digest("hex");if(actual!==manifest.sha256)throw new Error("Backup checksum mismatch");const safety=`${target}.before-restore-${Date.now()}`;if(fs.existsSync(target))fs.copyFileSync(target,safety);fs.copyFileSync(backup,target);process.stdout.write(`Restore completed. Previous database: ${safety}\n`);
