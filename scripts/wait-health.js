const url=process.argv[2]||"http://127.0.0.1:3000/api/health/ready",deadline=Date.now()+60_000;async function wait(){while(Date.now()<deadline){try{const response=await fetch(url);if(response.ok){process.stdout.write("Application ready\n");return}}catch{}await new Promise(resolve=>setTimeout(resolve,1000))}throw new Error("Application readiness timeout")}wait().catch(error=>{process.stderr.write(`${error.message}\n`);process.exitCode=1});
