const fs=require("node:fs"),path=require("node:path"),os=require("node:os"),crypto=require("node:crypto");const source=path.resolve(__dirname,"..","prisma","dev.db"),dir=fs.mkdtempSync(path.join(os.tmpdir(),"nuvra-restore-test-")),backup=path.join(dir,"backup.db"),restored=path.join(dir,"restored.db");try{fs.copyFileSync(source,backup);fs.copyFileSync(backup,restored);const hash=file=>crypto.createHash("sha256").update(fs.readFileSync(file)).digest("hex");if(hash(source)!==hash(restored))throw new Error("Restore verification failed");process.stdout.write("Disposable backup restore verified\n")}finally{fs.rmSync(dir,{recursive:true,force:true})}
