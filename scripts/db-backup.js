const fs=require("node:fs"),path=require("node:path"),crypto=require("node:crypto");const root=path.resolve(__dirname,".."),source=path.join(root,"prisma","dev.db"),dir=path.resolve(process.argv[2]||path.join(root,"backups"));if(!fs.existsSync(source))throw new Error("SQLite database not found");fs.mkdirSync(dir,{recursive:true});const stamp=new Date().toISOString().replace(/[:.]/g,"-");const target=path.join(dir,`nuvra-${stamp}.db`);fs.copyFileSync(source,target);const bytes=fs.readFileSync(target),sha256=crypto.createHash("sha256").update(bytes).digest("hex");const manifest={createdAt:new Date().toISOString(),source:"prisma/dev.db",file:path.basename(target),bytes:bytes.length,sha256};fs.writeFileSync(`${target}.json`,JSON.stringify(manifest,null,2));process.stdout.write(`${JSON.stringify(manifest)}\n`);
