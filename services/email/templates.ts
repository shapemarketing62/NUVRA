const shell=(title:string,body:string,action?:{label:string;url:string})=>({subject:title,text:`${title}\n\n${body}${action?`\n\n${action.label}: ${action.url}`:""}`,html:`<div style="font-family:Arial,sans-serif;max-width:560px;color:#172033"><h1 style="font-size:24px">${title}</h1><p style="line-height:1.6">${body}</p>${action?`<p><a href="${action.url}" style="display:inline-block;padding:12px 18px;background:#315efb;color:white;text-decoration:none;border-radius:8px">${action.label}</a></p>`:""}<p style="font-size:12px;color:#667085;margin-top:28px">Si no solicitaste esta acción, podés ignorar este mensaje.</p></div>`});
export const emailTemplates={verification:(name:string|undefined,url:string)=>shell("Confirmá tu email",`${name?`${name}, `:""}confirmá tu dirección para completar tu cuenta de NUVRA.`,{label:"Confirmar email",url}),passwordReset:(name:string|undefined,url:string)=>shell("Restablecé tu contraseña",`${name?`${name}, `:""}recibimos una solicitud para cambiar tu contraseña.`,{label:"Crear nueva contraseña",url}),invitation:(organization:string,inviter:string|undefined,url:string)=>shell(`Invitación a ${organization}`,`${inviter||"Un administrador"} te invitó a colaborar en NUVRA.`,{label:"Aceptar invitación",url}),alert:(title:string,message:string)=>shell(title,message),billing:(title:string,message:string)=>shell(title,message)};
